"""
LEGACY — DO NOT IMPORT.

This file used to sit at the project root as ``config.py`` and could shadow the
real ``config/`` package on some Python runtimes. Kept only for git history
reference. Always use:

    from config.config import get_config
"""
